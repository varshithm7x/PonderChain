{
  "name": "ponderchain-contracts",
  "version": "1.0.0",
  "description": "PonderChain Smart Contracts for Lisk Blockchain",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:lisk-sepolia": "hardhat run scripts/deploy.js --network liskSepolia",
    "deploy": "npm run deploy:lisk-sepolia",
    "verify": "hardhat verify --network liskSepolia",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "keywords": [
    "lisk",
    "blockchain",
    "smart-contracts",
    "prediction",
    "polling",
    "web3"
  ],
  "author": "PonderChain Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  }
}
